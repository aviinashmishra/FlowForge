# Stores Directory

This directory will contain Zustand stores for state management:

- `pipelineStore.ts` - Pipeline state and operations
- `collaborationStore.ts` - Real-time collaboration state
- `uiStore.ts` - UI state and preferences
- `executionStore.ts` - Pipeline execution state

Stores will be implemented in subsequent tasks.