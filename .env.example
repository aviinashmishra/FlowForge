# Database
DATABASE_URL="postgresql://username:password@localhost:5432/flowforge"

# Authentication Secrets (GENERATE NEW ONES - DO NOT USE THESE EXAMPLES)
JWT_SECRET="generate-with-node-scripts-generate-secrets-js"
NEXTAUTH_SECRET="generate-with-node-scripts-generate-secrets-js"
NEXTAUTH_URL="http://localhost:3000"

# JWT Configuration
JWT_EXPIRES_IN="7d"

# Email Configuration (for password reset)
EMAIL_FROM="noreply@flowforge.com"
EMAIL_SERVICE="smtp"
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER="your-email@gmail.com"
SMTP_PASS="your-app-password"

# App Configuration
NODE_ENV="development"